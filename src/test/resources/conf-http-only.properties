# ============================================================
# Test Configuration: HTTP Only (REST API)
# ============================================================

server.tcp.enabled=false
server.http.enabled=true
server.websocket.enabled=false

http.port=18081

bossThreadCount=1
workerThreadCount=2

so.backlog=100
so.reuseaddr=true
so.keepalive=true
tcp.nodelay=true

protocol.type=JSON
