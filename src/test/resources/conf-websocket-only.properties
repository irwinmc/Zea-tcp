# ============================================================
# Test Configuration: WebSocket Only
# ============================================================

server.tcp.enabled=false
server.http.enabled=false
server.websocket.enabled=true

web.socket.port=18300

bossThreadCount=1
workerThreadCount=2

so.backlog=100
so.reuseaddr=true
so.keepalive=true
tcp.nodelay=true

protocol.type=JSON
